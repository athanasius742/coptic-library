# Copy to .env (gitignored) and fill in. Loaded by the conversion tooling.

# Required for the PDF→EPUB conversion OCR step (tools/ct_ocr_batch.py).
# Used to OCR scanned / corrupt-text-layer Arabic pages via the Anthropic Batch
# API (Claude vision). The crawl/download and HTML→EPUB build tools do NOT need it.
ANTHROPIC_API_KEY=
